labels:
  backend: local

steps:
  - name: install
    image: bash
    commands:
      - npm ci

  - name: lint
    image: bash
    commands:
      - npm run lint

  - name: typecheck
    image: bash
    commands:
      - npx tsc --noEmit

  - name: react-doctor
    image: bash
    commands:
      - npm run react-doctor

  - name: test-unit
    image: bash
    commands:
      - npm run test

  - name: test-e2e
    image: bash
    commands:
      - npm ci
      - npx playwright install-deps
      - npm run test:e2e

  - name: deploy
    image: bash
    when:
      - event: push
        branch: main
    commands:
      - cd /home/fivem/apps/studios-web
      - chmod +x ./deploy.sh
      - ./deploy.sh
